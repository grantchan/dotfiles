set nocompatible                " Use Vim defaults instead of 100% vi compatibility

set number                      " Display line numbers on the left of each line
set hlsearch                    " Highlight searches

syntax on                       " Turn on color syntax highlighting
